package reversi;

public class ReversiController implements IController {

	IModel model;
	IView view;

	@Override
	public void initialise(IModel model, IView view)
	{
		this.model = model;
		this.view = view;
	}
	
	public void startup() {
		for (int y=0;y<8;y++) {
		     for (int x = 0; x < 8; x++) {
		    	model.setBoardContents(x, y, 0);   	     	 
		     }
		}
		
		model.setBoardContents(3, 3, 2);
		model.setBoardContents(4, 4, 2);
		model.setBoardContents(3, 4, 1);
		model.setBoardContents(4, 3, 1);
		
		model.setPlayer(1);
		model.setFinished(false);
		view.refreshView();
	}
	
	
	public void update() {
		
	}
	
	//@Override
	public void squareSelected( int player, int x, int y ) {
		System.out.print("Clicked tile\n");
		if (model.getBoardContents(x, y)==0) {
			if((model.getPlayer())==player) {
				System.out.print("Valid\n");
			}
			else if((model.getPlayer())!=player) {
				System.out.print("Not your turn\n");
			}
			
		}
		else if (model.getBoardContents(x, y)!=0) {
			if((model.getPlayer())==player) {
				System.out.print("Invalid location to play a piece\n");
			}
			else if((model.getPlayer())!=player) {
				System.out.print("Not your turn\n");
			}
			
		}
	}
	
	public void isMoveValid(int x, int y, int player) {
		if (player==1) {
			int other = 2;
		}
		else {
			int other = 1;
		}
	    for (int dx = -1; dx <= 1; dx++) {
	        for (int dy = -1; dy <= 1; dy++) {
	            if (dx == 0 && dy == 0) continue; // skip the current square

	            int cx = x + dx;
	            int cy = y + dy;
	            boolean seenOpponent = false;

	            while (cx >= 0 && cx < 8 && cy >= 0 && cy < 8) {
	                int piece = model.getBoardContents(cx, cy);

	                if (piece == opponent) {
	                    seenOpponent = true;
	                } else if (piece == player) {
	                    if (seenOpponent) return true; // Found opponent(s) followed by player
	                    else break; // Found player too soon
	                } else {
	                    break; // Found empty square
	                }

	                cx += dx;
	                cy += dy;
	            }
	        }
	    }
	}
	
	
	
	public void doAutomatedMove( int player ) {
		System.out.print("Clicked\n");
	}



}
